using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
namespace IEEECUSB
{
    class seeding
    {
        public seeding()
        {
            Controller cont = new Controller();
            //    //create committee
            //    if (cont.SelectReceivedRequests() == null)
            //    {
            //        for (int i = 0; i < 10; i++)
            //        {
            //            string Name = "Committee" + i.ToString();
            //            int k = cont.InsertCommittee(2019, Name);

            //        }
            //        //create volunteer
            //        for (int i = 0; i < 10; i++)
            //        {
            //            string Title = "V" + i.ToString();
            //            cont.InsertVolunteer(Title, i);

            //        }
            //        //create requests
            //for (int i = 0; i < 10; i++)
            //{
            //    string Title = "Request" + i.ToString();
            //    cont.InsertRequest(Title, i, 9 - i);

            //}
            //        //create tasks
            //        for (int i = 0; i < 10; i++)
            //        {
            //            string Name = "Task" + i.ToString();
            //            cont.InsertTask(Name, i, 9 - i, i);
            //        }
            //        //create event
            //        for (int i = 0; i < 10; i++)
            //        {
            //            string Title = "Event" + i.ToString();
            //            cont.InsertEvent(Title, i);
            //        }
            //    }
        }
    }
}
